// Quick script to clear rate limit data
console.log('Rate limit data would be stored in memory.');
console.log('Since the rate limiter uses in-memory storage, simply restart the backend server to clear all rate limits.');
console.log('\nAlternatively, wait 15 minutes for the rate limit window to expire.');
